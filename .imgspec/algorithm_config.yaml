# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algo_name: sister-prisma 
version: sister-dev
environment: ubuntu
repository_url: https://gitlab.com/geospec/sister.git 
docker_url: registry.imgspec.org/root/ade_base_images/enspec-sister:0.1

# fill out these fields
# explain what this algorithm does
description: Reformats native PRISMA L1 radiance HDF data to ENVI format
# path to the wrapper script for running the algorithm
run_command: sister/.imgspec/imgspec_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  - name: l1_zip
    type: file
  - name: output_resolution
    type: positional
